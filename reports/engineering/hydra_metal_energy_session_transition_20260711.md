# Immutable Research Task — Metal/Energy Session-Transition Pilot

Task ID: `eng_hydra_metal_energy_session_transition_20260711_v1`

Frozen before the new pilot reads any future target. Research only; Q4, paid
data, network, broker, and live execution are prohibited.

## Hypothesis

At the first 30 minutes of a Globex session, an overnight displacement from the
prior session close contains a stable, one-leg session-transition signal in
metals and energy that survives costs and transfers across GC/MGC and CL/MCL.
The signal is computed only from the prior session close and current session
open; it is not an inherited equity-index atom.

Discovery markets: GC and CL. Fresh micro transfer markets: MGC and MCL.
Development/falsification data ends at `2024-10-01`; Q4 remains sealed.

## Gates

Require prefix invariance, group-safe 30-bar targets, positive net result in
2024 Q1/Q2/Q3, at least 30 independent session days per market, conservative
round-turn costs, sign-flip and delayed controls, contract replication, and a
multiple-testing correction. Any mandatory failure kills the mechanism;
underpowered evidence is insufficient and never promoted.

No strategy, Topstep approval, holdout access, or shadow permission is implied
by a pilot pass. A pass only permits a separate sparse strategy audit.

## Acceptance

Exact repaired-map provenance, deterministic output, full pytest, no-lookahead,
compileall, governance, budget, Q4, registry, lock, and single-writer tests
must pass. Maximum automatic retries: 2.

Expected decision information value: `0.98`.
