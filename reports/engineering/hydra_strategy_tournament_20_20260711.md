# Immutable Task — Governed 20-Strategy Tournament

Task ID: `eng_hydra_strategy_tournament_20_20260711_v1`

The existing edge-atom discovery lab shall be executed only by the HYDRA
mission writer; no standalone registry writer is permitted. Development data
ends at `2024-10-01`; Q4 and future lockboxes remain sealed.

The tournament may propose at most 20 sparse strategies, each with at most
three mechanisms. Every strategy is recalculated from zero and must pass its
own candidate-level null suite, temporal and contract transfer, conservative
costs, adversarial validation, behavioral novelty, and shared-account Topstep
replay. No component inherits validation status. Clones and parameter-neighbor
variants do not count as distinct strategies.

The tournament may return fewer than 20 survivors. It must never manufacture a
pass by weakening a gate or by optimizing on Q4. No Q4, paid data, network,
broker, or live execution is authorized.

Expected decision information value: `0.995`.
