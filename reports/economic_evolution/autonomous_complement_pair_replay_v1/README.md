# Complement pair replay evidence

`economic_result.json` is the authoritative corrected replay.

`INVALID_DO_NOT_USE_epsilon_risk_charge_economic_result.json` is retained only as quarantined evidence of the rejected epsilon-risk run. It is non-promotional and must never be used for economic selection. `invalidation_receipt.json` records that invalidation.
